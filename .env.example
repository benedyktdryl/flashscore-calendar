# Optional — only required for "Add in background" (oauth) mode.
# Create OAuth client: Google Cloud → Credentials → Chrome extension → paste Extension ID.
WXT_GOOGLE_CLIENT_ID=123456789-xxxx.apps.googleusercontent.com

# Default mode is chosen in extension Options (google-url recommended for testing).
# No env var needed for ICS or Google Calendar URL modes.
